server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydemo1?serverTimezone=Asia/Tokyo&useSSL=false
    username: root
    password: Lwx014070!
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.demo.entity
  configuration:
    map-underscore-to-camel-case: true